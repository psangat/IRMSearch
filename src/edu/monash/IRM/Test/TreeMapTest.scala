package edu.monash.IRM.Test

/**
 * Created by psangat on 21/11/15.
 */

import scala.collection.immutable.TreeMap

object TreeMapTest {
  def main(args: Array[String]) {
    val treeMap = TreeMap("x" -> "foo1", "x" -> "foo2", "y" -> "foo3")
    //val subMap = treeMap

    //subMap.values.foreach(println)
  }
}
